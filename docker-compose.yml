services:
  app:
    build: .
    container_name: reading-soundtrack
    ports:
      - "80:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: always
